body {
  font-family: Arial, sans-serif;
  background: #dbeafe;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}
.weather-container {
  background: white;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
  text-align: center;
}
#city {
  padding: 8px;
  margin-bottom: 10px;
}
button {
  padding: 8px 12px;
}
#weatherResult {
  margin-top: 15px;
  font-size: 1.1em;
}
